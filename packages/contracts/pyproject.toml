[tool.poetry]
name = "yaga-contracts"
version = "0.1.0"
description = "Shared schemas for HTTP, A2A, events, and commands"
authors = ["YagaAi"]
packages = [{ include = "yaga_contracts" }]

[tool.poetry.dependencies]
python = "^3.12"
pydantic = "^2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
